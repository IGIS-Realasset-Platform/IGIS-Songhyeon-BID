# Web-to-PPTX 변환 표준 가이드라인 및 AI 프롬프트 엔지니어링 설계 사양서

- **문서 번호**: IGIS-STD-2026-V1.0
- **최종 개정일**: 2026년 6월 15일
- **공동 제정 및 권한자**: 기획추진센터
- **적용 대상**: 사내 웹 기반 프레젠테이션 산출물 전수

---

## 1. 변환 메커니즘 개요

웹 기반 프레젠테이션 사이트의 동적 컴포넌트들을 정확한 메타데이터 좌표로 분해하고, 이를 PPTX 파일 스펙에 부합하는 개별 도형 및 독자적인 텍스트 레이어로 완벽히 분리·재구성하는 메커니즘을 적용합니다. 이를 위해 Puppeteer 헤드리스 크롬 드라이버와 python-pptx 라이브러리를 연동한 자동화 파이프라인을 구축합니다.

---

## 2. 실무형 포팅 전략: 하이브리드-네이티브 결합 표준 모델

기존의 완전 네이티브 구현 방식(모든 컨텐츠의 카드, 구분선, 복잡한 인포그래픽을 AI가 직접 생성하는 형태)은 가변적 레이아웃 소화와 프레젠테이션 생동감 측면에서 구조적 한계가 존재합니다. 따라서 본 프로젝트는 **하이브리드-네이티브 결합 모델**을 표준 아키텍처로 채택합니다.

### 의사결정 Rationale (왜 하이브리드 결합 모델인가?)
1. **실무형 편집 범위 분석**: 보고 단계에서 발생하는 오타 정정, 수치 변경, 설명글 수정 요구는 매우 빈번하지만, 이미 합의된 인포그래픽이나 차트의 배치 자체를 수정하는 일은 극히 드뭅니다.
2. **제작 속도 및 재현도**: 디자인 재현도가 가장 정확한 스크린샷 배경 이미지 포팅을 근간으로 삼되, 변경 가능성이 높은 1:1 개별 텍스트 박스와 간단한 기본 구분선/도형 개체만 네이티브 객체로 얹어 실무적 편집성을 확보합니다.

### 변환 옵션 비교

| 변환 옵션 | 주요 메커니즘 | 실무적 장단점 및 채택 여부 |
| :--- | :--- | :--- |
| **옵션 1: 완전 네이티브** | 모든 카드, 그래픽 개체, 차트를 PPTX 도형 스펙으로 100% 매핑 | 수정 범위는 넓으나 디자인이 단조로워지고 구현 속도가 느림. (비채택) |
| **옵션 2: 하이브리드 결합<br>(표준 모델)** | 고해상도 템플릿을 배경 이미지로 얹고, 1:1 편집용 텍스트박스 및 기본 구분선을 네이티브로 배치 | 디자인 정합성이 100% 보장되며 텍스트 편집이 가능하고 제작 속도가 극도로 빠름. (기획추진센터 공식 표준 채택) |

---

## 3. 변환 업무 지시용 표준 AI 프롬프트 규격

AI 어시스턴트에게 새로운 웹-to-PPTX 변환 스크립트 제작을 지시하거나 알고리즘 수정을 요구할 때 아래의 규칙을 준수하여 오류를 방지합니다.

### 필수 포함 5대 지시 제약 조건
1. **1:1 개별 텍스트 박스 분리**: 텍스트 수평/수직 병합 알고리즘을 배제하고 모든 텍스트를 독립적인 textbox로 처리합니다.
2. **텍스트 투명도 기반 배경 캡처**: 텍스트 은닉 시 `visibility:hidden` 대신 `color:transparent`를 적용하여 배경 박스와 테두리 디자인을 보존합니다.
3. **정렬축 보정 가로폭 버퍼**: left, center, right 정렬 상태에 따른 기하학적 기준축 보정 및 너비 확장 수식을 적용합니다.
4. **인라인 서식 예외 트리 필터**: `STRONG`, `SPAN` 등 인라인 태그가 포함된 본문은 부모 단락 묶음 단위를 유지합니다.
5. **색상/서체 변환 보정 필터**: oklch 색상을 RGB 표준 형식으로 정규화하고 서체는 "Pretendard Variable" 폰트를 강제 적용합니다.

### 표준 AI 지시 프롬프트 템플릿
```markdown
[Role] 너는 프레젠테이션 웹 사이트를 PPTX/Keynote용 슬라이드로 변환하는 전문 프론트엔드/자동화 엔지니어다.
[Task] React 웹 슬라이드 주소를 로드하여 편집 가능한 개별 텍스트 레이어가 얹혀진 PPTX 파일 생성 파이프라인을 작성하라.
[Strict Rules]
1. (배경 캡처) 텍스트를 은닉해 배경을 캡처할 때, H1~H6, P, SPAN, STRONG 등 모든 텍스트 태그에 style.color = 'transparent'를 부여하라. style.visibility = 'hidden'은 사용하지 마라. 배경 박스와 테두리 선(Border), 그림자(Shadow)는 온전히 배경 이미지에 포함되어야 한다.
2. (레이아웃 추출) DOM 분석 시 STRONG, SPAN, B, EM, A 같은 인라인 태그는 자식 노드의 좌표를 쪼개지 말고, 부모 노드(LI, P, H1~H6 등) 단위로 전체 문장 텍스트와 좌표를 수집해 문장 유실을 원천적으로 막아라.
3. (텍스트 정렬 보정) 파워포인트 빌드 시 텍스트 상자 너비를 웹의 1.45~1.8배로 확장하되, 정렬선 훼손을 방지하기 위해 다음 수식을 적용하여 X 좌표를 보정하라.
   - Left: left = original_x
   - Center: left = (original_x + original_w / 2) - new_w / 2
   - Right: left = (original_x + original_w) - new_w
4. (서체 및 색상) PPTX 텍스트의 Paragraph와 Run 수준 글꼴을 "Pretendard Variable"로 강제 통일하고, oklch 포맷 색상은 rgb 표준 형식으로 변환하여 빌더에 제공하라.
```

---

## 4. 핵심 기술 해결 방안 및 아키텍처 명세

### A. 설명글 유실 방지: 태그 인지형 중복/계층 필터링 (extract_layout.js)
텍스트 노드가 다른 하위 자식 노드를 포함하고 있을 때, Bounding Box 오차 범위(8px) 이내에서 두 노드의 포함 관계를 비교해 필터링을 수행합니다.

| 태그 성격 및 종류 | 필터 작동 규칙 | 설계 목적 및 결과 |
| :--- | :--- | :--- |
| **인라인 태그**<br>(SPAN, STRONG, B, EM, A) | 자식 노드를 제거하고 부모 문장 노드를 보존 | 문장 중간에 강조(bold)나 링크가 섞여 있어도 문장이 쪼개지지 않고 통째로 복원됨. |
| **블록 태그**<br>(DIV, P, LI, H1~H6) | 부모 컨테이너를 제거하고 개별 자식 노드를 보존 | 독립적인 데이터 카드나 단락들이 엉키지 않고 각각의 텍스트 박스로 개별 분리됨. |

### B. 중첩 카드 및 테두리(Border) 누락 방지 (capture_backgrounds.js)
큰 상자 내에 작은 박스 형태의 카드 UI가 들어있는 경우, `visibility: hidden` 대신 **Text Transparency Strategy(텍스트 투명화 제어)**를 사용합니다.
- `el.style.color = 'transparent'`를 부여하여 글자만 감추고 물리 영역 및 테두리(`border`), 그림자(`box-shadow`), 배경색(`background-color`)은 100% 정상 작동하도록 유지하여 디자인 레이아웃을 보존합니다.

### C. 텍스트 너비 확장 및 정렬 정합성 보정 (build_pptx_*.py)
웹 자간과 PPTX 렌더링 엔진 자간 오차로 인한 강제 개행(줄바꿈)을 방지합니다.
- **너비 확장 버퍼 규칙**: 15자 미만의 짧은 텍스트는 `width * 1.45`, 15자 이상의 긴 본문/설명 텍스트는 `width * 1.80`을 적용합니다.
- **중심축 정렬선 유지 공식**:

| 정렬 방식 | 텍스트 정렬선 수식 알고리즘 (Python 코드 규격) | 비주얼 효과 |
| :--- | :--- | :--- |
| **Left (좌측 정렬)** | `left = original_x`<br>`width = (original_w * factor) + 40` | 좌측 시작점은 고정되고 우측 공간만 확장됨 |
| **Center (중앙 정렬)** | `cx = original_x + original_w / 2`<br>`left = cx - (original_w * factor) / 2` | 텍스트의 기하학적 중심축을 고정한 채 양방향으로 확장됨 |
| **Right (우측 정렬)** | `right_edge = original_x + original_w`<br>`left = right_edge - (original_w * factor)` | 우측 끝 정렬선을 완벽하게 유지하며 좌측 방향으로 확장됨 |

### D. oklch 색상 컴포팅 및 Pretendard Variable 서체 이중 적용
1. **색상 표준화 파서**: 웹 문서에 임시 Div(`temp-color-converter`)를 상주시켜 oklch 값을 표준 `rgb(r,g,b)` 형식으로 변환하여 레이아웃 데이터에 적재합니다. 실패 시 예외 필터를 통해 브랜드 지정 블루(`#1e3a8a`)로 자동 치환합니다.
2. **이중 서체 주입**: Keynote 임포트 시 서체 깨짐 방지를 위해 단락(`paragraph.font.name`)과 런(`run.font.name`) 양쪽에 `"Pretendard Variable"` 글꼴을 이중으로 강제 설정합니다.

---

## 5. 변환 품질 관리(QA) 표준 가이드

변환이 완료된 PPTX 파일 배포 전, 수집 데이터 검수 스크립트를 통해 다음의 기준을 전수 검증합니다.
- **문장 유실 체크**: 1글자 단위 노드가 과도하게 분절되지 않았으며 리스트 기호만 남고 설명문이 누락된 장표가 없을 것.
- **정렬축 체크**: 동일 영역(오차 범위 5px) 내에서 텍스트가 서로 포개어지는 박스가 없을 것.
